"use client";

/**
 * CONTRACT STUB — the compose UI replaces the body; the props are frozen.
 *
 * Publishing runs in the background while the default translations are
 * attempted, so the author can keep using the app. The feed shell renders this
 * banner above the cards to show "Posting…", the success hand-off to the new
 * post, or the failure state with retry (body and image kept).
 */
export type PublishStatusBannerProps = {
  locale: string;
};

export default function PublishStatusBanner(props: PublishStatusBannerProps) {
  void props;
  return null;
}
